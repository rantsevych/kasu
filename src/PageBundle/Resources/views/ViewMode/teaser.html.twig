<div class="fields">
    <h2 class="field field-title">{{ item.title }}</h2>
    <div class="field field-body">{{ item.body|raw }}</div>
    {% set path = path('page_view', {'id' : item.id }) %}
    <div class="field field-page-link"><a href="{{ path }}">Детальніше</a></div>
</div>